# Система учета грузов - Версия 4.0

## 🚀 Описание
Современная веб-система для учета грузоперевозок с адаптивным дизайном и мягкими анимациями.

## 📁 Структура проекта
```
Cargo Tracker/
├── index.html          # Главная HTML страница
├── styles.css          # Стили CSS (версия 4.0)
├── script.js           # JavaScript логика (версия 4.0)
├── server.js           # Node.js сервер
├── package.json        # Зависимости проекта
├── data/
│   └── shipments.json  # Локальное хранение данных
├── logo.png           # Логотип (светлый)
├── logo_dark.png      # Логотип (темный)
└── README.md          # Документация
```

## ⚡ Быстрый запуск

### 1. Установка зависимостей
```bash
npm install
```

### 2. Запуск сервера
```bash
npm start
# или
node server.js
```

### 3. Открытие в браузере
```
http://localhost:3000
```

## 🔄 Обновление проекта

### Получение последних изменений
```bash
# Перейти в папку проекта
cd cargo-tracker

# Получить последние изменения
git pull origin main

# Установить/обновить зависимости
npm install

# Перезапустить сервер
npm start
```

### Проверка версии
```bash
# Посмотреть последние коммиты
git log --oneline -5

# Проверить текущую ветку
git branch

# Проверить статус файлов
git status
```

### Для разработчиков - внесение изменений
```bash
# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить в репозиторий
git push origin main
```

## 🎨 Возможности

### ✨ Основные функции
- ➕ **Добавление отправок** - полная форма с валидацией
- ✏️ **Редактирование** - изменение существующих записей
- 🗑️ **Удаление** - с подтверждением безопасности
- 🔍 **Поиск и фильтрация** - по датам, статусу, городам
- 📱 **Мобильная версия** - адаптивный дизайн

### 🎨 Интерфейс
- 🌙 **Темная/светлая тема** - переключение одной кнопкой
- ✨ **Мягкие анимации** - плавные переходы и эффекты
- 📊 **Карточки на мобильных** - удобный просмотр данных
- 🎯 **Автокомплит городов** - Владивосток, Хабаровск, Южно-Сахалинск, Благовещенск

### 🔧 Технические особенности
- 💾 **Локальное хранение** - данные в JSON файле
- 🚀 **REST API** - стандартные HTTP методы
- 📱 **Responsive** - работает на всех устройствах
- ⚡ **Быстродействие** - оптимизированный код

## 🛠️ API Endpoints

### GET /api/shipments
Получение всех отправок
```json
[
  {
    "id": "1638360000000",
    "shippingDate": "2023-12-01",
    "shippingCity": "Владивосток",
    "shipperName": "Иванов И.И.",
    "deliveryDate": "2023-12-05",
    "deliveryCity": "Хабаровск",
    "receiverName": "Петров П.П.",
    "carrier": "ТК Восток",
    "packages": 5,
    "description": "Бытовая техника",
    "status": "delivered"
  }
]
```

### POST /api/shipments
Сохранение массива отправок
```json
{
  "status": "success",
  "message": "Данные сохранены"
}
```

### DELETE /api/shipments
Очистка всех данных
```json
{
  "status": "success",
  "message": "Все данные удалены"
}
```

### GET /health
Проверка состояния сервера
```json
{
  "status": "OK",
  "timestamp": "2023-12-01T10:00:00.000Z",
  "uptime": 3600.123
}
```

## 📊 Структура данных

### Поля отправки
| Поле | Тип | Обязательное | Описание |
|------|-----|-------------|----------|
| id | string | да | Уникальный идентификатор |
| shippingDate | string | да | Дата отправки (YYYY-MM-DD) |
| shippingCity | string | да | Город отправления |
| shipperName | string | да | ФИО отправителя |
| deliveryDate | string | нет | Дата получения |
| deliveryCity | string | нет | Город получения |
| receiverName | string | нет | ФИО получателя |
| carrier | string | да | Перевозчик |
| packages | number | да | Количество мест |
| description | string | да | Описание груза |
| status | string | авто | pending/delivered |

## 🔄 Автоматические функции

### Статус отправки
- `pending` - если нет даты получения
- `delivered` - если есть дата получения

### Фильтр по умолчанию
При загрузке автоматически устанавливается фильтр на последние 7 дней.

### Валидация
- Обязательные поля проверяются на клиенте
- Количество мест должно быть > 0
- Даты в корректном формате

## 🎨 Темы оформления

### Светлая тема (по умолчанию)
- Белый фон
- Темный текст
- Синие акценты

### Темная тема
- Темно-серый фон
- Светлый текст
- Голубые акценты

Переключение кнопкой 🌞/☀️ в правом верхнем углу.

## 📱 Мобильная версия

### Особенности
- Карточный вид вместо таблицы
- Скрываемые фильтры
- Увеличенные кнопки
- Оптимизированная навигация

### Активация
Автоматически при ширине экрана < 768px

## 🚀 Деплой

### Локальный VPS
```bash
# Клонирование
git clone https://github.com/sv3t0v1k/cargo-tracker.git
cd cargo-tracker

# Установка
npm install

# Запуск в продакшн
npm start
```

### Systemd сервис (Linux)
```ini
[Unit]
Description=Cargo Tracker
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/path/to/cargo-tracker
ExecStart=/usr/bin/node server.js
Restart=always

[Install]
WantedBy=multi-user.target
```

## 🔧 Настройка

### Порт сервера
По умолчанию: `3000`
Изменить в `server.js`:
```javascript
const PORT = process.env.PORT || 3000;
```

### Города для автокомплита
В файле `index.html`:
```html
<datalist id="cities">
    <option value="Владивосток">
    <option value="Хабаровск">
    <option value="Южно-Сахалинск">
    <option value="Благовещенск">
    <!-- Добавить свои города -->
</datalist>
```

## 🐛 Отладка

### Логи
Сервер выводит информацию в консоль:
```bash
node server.js
# Сервер запущен на порту 3000
# http://localhost:3000
```

### Проверка API
```bash
# Проверка здоровья
curl http://localhost:3000/health

# Получение данных
curl http://localhost:3000/api/shipments
```

### Браузер
Откройте Developer Tools (F12) для просмотра ошибок JavaScript.

## 📝 Журнал изменений

### Версия 4.0 (текущая) - Декабрь 2024
- ✅ **Разделение на файлы**: HTML, CSS, JS вынесены в отдельные файлы
- ✅ **Исправление багов**: все кнопки теперь кликабельны
- ✅ **Мобильная версия**: корректное отображение контента и фильтров
- ✅ **Оптимизация анимаций**: более плавные переходы
- ✅ **Улучшенная структура**: чистый и понятный код
- ✅ **Обновленная документация**: подробные инструкции по установке и обновлению

### Версия 3.0
- ✅ Анимации и переходы
- ✅ Темная тема
- ✅ Мобильные карточки

### Версия 2.0
- ✅ Локальное хранение
- ✅ REST API
- ✅ Фильтрация

### Версия 1.0
- ✅ Базовый функционал
- ✅ CRUD операции

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (F12)
2. Убедитесь что сервер запущен
3. Проверьте доступность порта 3000

## 🌐 Ссылки

- **Репозиторий**: https://github.com/sv3t0v1k/cargo-tracker
- **Демо**: http://localhost:3000 (после запуска)
- **Версия**: 4.0
- **Последнее обновление**: Декабрь 2024

## 📦 Установка из GitHub

### Первая установка
```bash
# Клонировать репозиторий
git clone https://github.com/sv3t0v1k/cargo-tracker.git

# Перейти в папку
cd cargo-tracker

# Установить зависимости
npm install

# Запустить
npm start
```

### Обновление существующей установки
```bash
# В папке проекта
git pull origin main
npm install
npm start
```

## 📄 Лицензия
MIT License - свободное использование и модификация.

---
**Система готова к использованию! 🚀** 